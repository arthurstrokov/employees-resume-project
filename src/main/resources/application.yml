spring:
  application:
    name: employees-resume-project
  profiles:
    active: development # production development
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      enabled: true
      failFast: false
      uri: http://172.17.0.2:8888/ # only for local using
